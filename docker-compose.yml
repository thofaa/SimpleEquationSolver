services:
  solver:
    build:
      context: .
      dockerfile: src/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./src/static:/app/static:ro
      - ./src/solver.py:/app/solver.py:ro
    restart: unless-stopped
